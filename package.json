{
  "name": "vite-webext",
  "version": "1.2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "format": "prettier --write .",
    "watch": "turbo run watch"
  },
  "devDependencies": {
    "prettier": "latest",
    "prettier-plugin-imports": "latest",
    "turbo": "latest"
  }
}
