{
  "name": "vite-webext",
  "version": "1.2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "format": "prettier . -w -u --cache"
  },
  "devDependencies": {
    "husky": "^9",
    "lint-staged": "^15",
    "prettier": "latest",
    "prettier-plugin-imports": "latest",
    "prettier-plugin-pkg": "latest",
    "turbo": "latest"
  },
  "lint-staged": {
    "*": "prettier -w -u --cache"
  }
}
